 git add fil name
 
 #push our rewitten,smaller commit
 
 git rm fil name
 
 #includ < docker fil >
 }
 int main < void >
 
 $ git input = (hello, world\n")
  }
  $printf (hello, world);
  {
  git strang = >git input (hello, word);
  
 $ ./.a.out);
 
